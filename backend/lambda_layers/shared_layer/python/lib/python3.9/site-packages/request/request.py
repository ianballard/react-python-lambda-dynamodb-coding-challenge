from service.logs import Log, LogEventType


def api_endpoint():
    def decorator(func):
        def wrapper(event, context):

            Log(data=event, event_type=LogEventType.APIRequest).info()

            return func(event, context)

        return wrapper

    return decorator
